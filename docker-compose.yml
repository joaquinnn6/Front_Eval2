version: '3.8'

services:
  frontend:
    build: .
    container_name: frontend-app
    restart: always
    environment:
      PORT: 5000
      DEBUG: "False"
      BACKEND_URL: http://localhost:3000
      SECRET_KEY: clave_secreta_segura_2025
    ports:
      - "5000:5000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge